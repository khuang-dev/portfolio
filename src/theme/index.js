import { createMuiTheme } from "@material-ui/core/styles";

export default createMuiTheme({
  palette: {
    primary: {
      main: "#ffffff",
    },
  },
  fonts: {
    primary: {
      main: "Raleway, sans-serif",
    },
    secondary: {
      main: "Homemade Apple, cursive",
    },
  },
});
